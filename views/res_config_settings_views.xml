<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.hr.payroll</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="45" />
        <field name="inherit_id" ref="hr_payroll.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@id='hr_payroll_settings']" position="after">
                <block title="Gestion des congés" id="hr_payroll_leaves_settings">
                    <setting string="Attribution automatique de congés" 
                             help="Ajouter automatiquement une attribution de congés lors de la validation des bulletins">
                        <field name="add_leave_attribution_when_done"/>
                    </setting>
                    <setting string="Validation automatique des congés" 
                             help="Valider automatiquement les congés enregistrés sur le bulletin lors de sa validation">
                        <field name="validate_leave_when_done"/>
                    </setting>
                    <setting string="Type de congés payés" 
                             help="Définir le type de congés à utiliser pour les congés payés">
                        <div class="content-group">
                            <div class="mt16">
                                <field name="holiday_code" placeholder="Sélectionner le type de congés"/>
                            </div>
                        </div>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>